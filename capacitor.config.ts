import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.wanderinghedgehognudge',
  appName: 'wandering-hedgehog-nudge',
  webDir: 'dist'
};

export default config;
